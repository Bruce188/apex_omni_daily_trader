name: ApexOmni Daily Trade

on:
  schedule:
    - cron: '0 14 * * *' # Runs at 14:00 UTC daily
  workflow_dispatch:

jobs:
  run-bot:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Build Bot Image
        run: docker build -t apex-bot .

      - name: Run Trading Bot
        env:
          # --- Secrets (Add these in GitHub Settings) ---
          APEX_API_KEY: ${{ secrets.APEX_API_KEY }}
          APEX_ZK_SEEDS: ${{ secrets.APEX_ZK_SEEDS }}
          
          # --- Public Config (Can be hardcoded here) ---
          APEX_NETWORK: mainnet
          DRY_RUN: false
          LOG_LEVEL: INFO
        run: |
          docker run --rm \
            -e APEX_API_KEY="$APEX_API_KEY" \
            -e APEX_ZK_SEEDS="$APEX_ZK_SEEDS" \
            -e APEX_NETWORK="$APEX_NETWORK" \
            -e DRY_RUN="$DRY_RUN" \
            -e LOG_LEVEL="$LOG_LEVEL" \
            apex-bot
