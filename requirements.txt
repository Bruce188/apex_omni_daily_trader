# ApexOmni Trading Bot - Python Dependencies
# ==========================================
# Pinned versions for reproducible builds

# Core SDK
apexomni>=3.0.0

# ApexOmni SDK dependencies (not declared by the package)
# These are required for the SDK to function properly
web3>=6.0.0
numpy>=1.24.0
ecdsa>=0.18.0
sympy>=1.11,<1.13
mpmath>=1.3.0
python-dateutil>=2.8.0

# Configuration
python-dotenv==1.0.1
pyyaml==6.0.1

# HTTP requests (usually installed with SDK, but explicit)
requests==2.31.0

# Scheduling (optional, for automated runs)
schedule==1.2.1

# File locking for concurrent access (REQUIRED, not optional)
filelock==3.13.1

# Development dependencies (optional)
# pytest==7.4.3
# pytest-cov==4.1.0
# black==23.12.1
# flake8==6.1.0
