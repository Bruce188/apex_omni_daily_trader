# ApexOmni Trading Bot - Environment Variables
# =============================================
#
# IMPORTANT: Copy this file to .env and fill in your actual credentials.
# NEVER commit the .env file to version control!
#
# To create your .env file:
#   cp .env.example .env
#   nano .env  # or your preferred editor
#

# ===== API CREDENTIALS =====
# Generate these from: https://omni.apex.exchange/keyManagement
# Or for testnet: https://testnet.omni.apex.exchange/keyManagement

# Your API Key (required)
APEX_API_KEY=your_api_key_here

# Your API Secret (required)
APEX_API_SECRET=your_api_secret_here

# Your API Passphrase (required)
# This is the passphrase you set when creating the API key
APEX_PASSPHRASE=your_passphrase_here

# ===== ZK KEYS (Required for Trading) =====
# These are needed to sign orders on the zkLink layer.
# Get these from: Key Management page > "Omni Key" button

# ZK Seeds (required for placing orders)
APEX_ZK_SEEDS=your_zk_seeds_here

# ZK L2 Key (can be left empty, will be derived from seeds)
APEX_ZK_L2KEY=

# ===== NETWORK SETTINGS =====
# Network: "testnet" or "mainnet"
# ALWAYS test on testnet first!
APEX_NETWORK=testnet

# Alternative: Use this flag (overrides APEX_NETWORK)
# APEX_TESTNET=true

# ===== SCHEDULE CONFIGURATION =====
# Mode: "daily" (single trade per day) or "continuous" (interval-based 24/7)
# For Docker deployment, "continuous" is recommended
SCHEDULE_MODE=continuous

# Hours between trades (for continuous mode, 1-24)
TRADE_INTERVAL_HOURS=4

# ===== SAFETY OVERRIDES =====
# Force dry-run mode (overrides config file setting)
# Set to "false" for live trading (after testing!)
DRY_RUN=true

# ===== LOGGING =====
# Log level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# ===== DEBUG & MONITORING =====
# Enable detailed error messages (default: false)
# Set to true for development/troubleshooting
DEBUG=false

# ===== CIRCUIT BREAKER =====
# Maximum consecutive failures before halting trading (default: 5)
MAX_FAILURES=5

# Minutes to wait before attempting recovery (default: 30)
CIRCUIT_RESET_MINUTES=30

# ===== HARDCODED SAFETY (NOT CONFIGURABLE) =====
# The following settings are hardcoded and cannot be changed:
# - Leverage: Always 1x (cross margin)
# - Position Closing: Always immediate
# These ensure safe operation and eliminate liquidation/exposure risk.
